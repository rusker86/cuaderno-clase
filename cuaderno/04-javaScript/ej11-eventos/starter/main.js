// Ej11 — Eventos (starter)

// 1) Click: #btn-click → incrementa #count
// TODO: addEventListener('click', ...)

// 2) Input en vivo: #name → actualiza #preview
// TODO: addEventListener('input', ...)

// 3) Submit: #demo-form → preventDefault + validar #email
// TODO: addEventListener('submit', ...)

// 4) Teclado global: document → keydown muestra en #last-key
// TODO: addEventListener('keydown', ...)
// (Opcional) botón #toggle-keys activa/desactiva el listener

// 5) Delegación: #list → si target.matches('.del') elimina <li>
// TODO: addEventListener('click', ...)

// 6) Listener con { once: true } en #btn-once
// TODO: addEventListener('click', handler, { once: true })

// 7) (Opcional) Propagación: .card y #inner-btn → stopPropagation en el botón
// TODO: addEventListener en '#bubble-card' y '#inner-btn'
